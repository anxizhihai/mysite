import Debug "mo:base/Debug";
// import Nat "mo:base/Nat"
import Array "mo:base/Array";


// func fib(n: Nat): Nat {
// if (n <= 1) 1 else fib(n - 2) + fib(n - 1); 
// };

// Debug.print(Nat.toText(fib(10)));

// func inc(n: Nat) : Nat {
//    return n
//  };

// Debug.print(Nat.toText(inc(2)));
let xs : [var Nat] = [4, 2, 6, 1, 5];
func inc(xs) :  [Int] {
    array
 };

inc([1,28,2,5,9]);